# MySQL Export Configuration
# Copy this file to .env.export and modify the values for your setup

# Database Connection Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=mysqlpassword
DB_DATABASE=dbemb
DB_PORT=3306

# Export Settings
EXPORT_DIR=../exports
INCLUDE_SYSTEM_USERS=true
INCLUDE_PRIVILEGES=true

# Output Format
PRETTY_JSON=true
INCLUDE_TIMESTAMPS=true