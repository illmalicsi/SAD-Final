// Legacy filename kept for compatibility. Re-export the real authService so
// any remaining imports will use the actual backend implementation.
import authService from './authService';

export default authService;